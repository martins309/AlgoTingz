//radix sort helper methods

/*
use the getDigit(num, place)
returns the digit in num at the given place value
*/




function getDigit(num, place){
    return Math.floot(Math.abs(num) / Math.pow(10, i) % 10)
}